version: 3

agent:
  authtoken: ${NGROK_AUTHTOKEN}
  web_addr: 0.0.0.0:4040

endpoints:
  - name: prometheus
    url: ""            # ← blank = assign a random *.ngrok.app HTTP URL
    upstream:
      url: 0.0.0.0:9090
