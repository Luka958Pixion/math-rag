default:
  default:
    basic:
      max_time: 60.0
      max_num_retries: 6

openai:
  omni-moderation-latest:
    concurrent:
      max_requests_per_minute: 1000.0
      max_tokens_per_minute: 50000.0
